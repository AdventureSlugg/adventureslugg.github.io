---
import '../../assets/css/styles.css';
import '../../assets/css/rainbowEffect.css'
---

<div class="tool-bar row">
	<div class="window-options">
		<button 
			id="exit-option"
			class="exit window-option clickable"
			title="Exit"
		> x </button>
		<button 
			id="hide-option"
			class="hide window-option clickable"
			title="DO NOT press this button"
		> _ </button>
		<button 
			id="minimize-option"
			class="minimize window-option clickable"
			title="Huh... wonder what this does?"
		> ^ </button>
	</div>

	<div class="page-title">
		<p class="rainbow">
			
			<span>
				Welcome to Zoe's Playground!
			</span>
		</p>
	</div>

	<div class="theme-options">
		<div class="form-check form-switch">
			<input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
		</div>
	</div>
</div>

<style>
	@keyframes bounce {
	0%, 100% {
		transform: translateY(0vw);
	}
	20% {
		transform: translateY(-1vw);
	}
	40% {
		transform: translateY(0vw);
	}
	}
	.rainbow:hover {
		animation: bounce 1.5s ease-in-out;
	}
	
	.page-title {
		margin:auto; 
		color: white;
	}
	.tool-bar {
		background-color: var(--quarternary-bg-color);
		height: 3vw;
		width: 100%;
	}

	.window-options {
		padding: .75vw;
	}

	.theme-options {
		padding: .75vw;
		margin-right: 0vw;
	}

	.window-option {
		border-radius: 5vw;
		height: 1.5vw;
		width: 1.5vw;
		border: 0px;
	}

	.window-option:hover {
		opacity: .5;
	}

	#exit-option {
		background-color: #FE5F57;
		color: #8c0700;
	}

	#hide-option {
		background-color: #FEBC2E;
		color: #ac7500;
	}

	#minimize-option {
		background-color: #28C840;
		color:#007712
	}
</style>